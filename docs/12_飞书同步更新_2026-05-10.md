## 进度更新（2026-05-10）

### 本轮完成

- 清理后端历史遗留无用代码：
  - 删除未被运行链路引用的附件上下文服务 `attachment_context_service.py`
  - 删除已废弃“清空当前会话消息”后端接口（`DELETE /conversations/{id}/messages`）及对应 service 方法
  - 清理上传路由中未使用的数据库依赖参数
- 修复文件问答关键 bug：
  - 聊天接口文件类型判断改为基于 `file_name` 扩展名识别
  - 修复此前所有文件附件被误判不支持的问题
- 完成文档类型支持落地：
  - 后端已支持 `txt / md / pdf / docx` 解析
  - 前端上传 `accept` 与后端能力已对齐
- 优化上下文诊断展示交互：
  - 由输入区常驻卡片改为“上下文”按钮弹层，减少主聊天区占位

### 文档同步

- 已更新：
  - `05_详细需求清单.md`
  - `06_技术实现设计.md`
  - `07_当前实现进展与下一步计划.md`

### 回归验证

- 后端：`py_compile` + `ensure_runtime_schema()` 通过
- 前端：`npm run lint`、`npm run build` 通过

