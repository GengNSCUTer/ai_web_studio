# 2026-05-28 上下文治理文档与技能规范补充

## 本次同步范围

本轮主要不是新增业务功能，而是补齐“上下文治理理解层”和“后续协作规范层”。

同步更新的本地文件：

- `docs/08_上下文治理研究与实施路线.md`
- `docs/09_上下文配置与动态预算说明.md`
- `AGENTS.md`

其中飞书同步的核心文档为：

- `08_上下文治理研究与实施路线`
- `09_上下文配置与动态预算说明`

`AGENTS.md` 属于项目本地协作规范文件，不单独覆盖到飞书对应技术文档中，但本次摘要会在项目总览里说明其变化。

## 本轮文档补充内容

### 1. 对齐主流工程代理 / 大模型产品的上下文治理做法

在 `08_上下文治理研究与实施路线.md` 中，补充了以下对标内容：

- OpenAI Prompt Caching / stable prefix / `prompt_cache_key` 的工程含义
- Claude Code 的上下文分层思路：聊天历史、文件内容、工具输出、项目规范、memory、skills
- Codex / Codex CLI 的 `AGENTS.md`、skills、结构化 handoff 思路

核心结论是：

- 上下文治理不是“只截断历史消息”
- 工程结构、稳定前缀、可外置知识、按需加载能力，本身就是上下文治理的一部分

### 2. 对齐当前项目已经落地的真实实现

在 `08_上下文治理研究与实施路线.md` 中，新增了对当前代码状态的总结：

- 动态预算规划器
- 历史消息裁剪
- 增量摘要刷新
- 长期记忆注入
- 附件上下文治理
- 外部工具上下文治理
- tokenizer 级观测
- 稳定前缀 hash 观测

同时也补充了当前仍缺失的能力：

- 严格 token-first 裁剪
- provider 真实缓存命中 telemetry
- 知识库级召回接入治理主链路
- 记忆冲突治理和衰减
- 更强的上下文诊断产品化

### 3. 对齐当前真实预算参数和诊断字段

在 `09_上下文配置与动态预算说明.md` 中，补充了：

- 当前预算公式的真实代码含义
- 图片等价 token / chars 预算
- 摘要刷新条件
- `prompt_prefix_hash` / `prompt_prefix_tokens` / `prompt_prefix_reused_last_turn`
- tokenizer 编码估算
- 前端上下文诊断面板目前实际展示的字段
- 哪些参数适合用户配置，哪些参数应继续留在后端控制

当前文档已明确：

- 现阶段是“token 预算输入 + 字符预算执行 + tokenizer 观测回传”的混合框架
- 这已经明显强于纯字符裁剪，但还不是最终 token-first 形态

### 4. 补充项目协作规范中的代码健康巡检要求

在 `AGENTS.md` 中新增：

- “代码健康巡检要求”章节
- 建议在大功能完成后、核心文件明显膨胀后、准备进入下一阶段前定期做结构巡检
- 当前建议优先使用的 skill：
  - `vercel-react-best-practices`
  - `next-best-practices`
  - `webapp-testing`
  - `find-skills`
  - `autoresearch`

同时明确写明：

- 目前没有找到一个高质量、可直接安装、且专门覆盖“通用代码审查 + 无用代码清理 + 架构瘦身”的现成 skill
- 当前更稳的策略是：专项 skill + 人工 code review

## 当前阶段判断

如果从项目能力角度看：

- 第一阶段上下文治理工程化已经基本完成
- 第二阶段知识库级上下文治理还未开始正式开发
- 第三阶段 provider cache telemetry / 记忆精细治理仍在后续规划中

因此下一步仍建议回到主线开发：

1. 独立设置页产品化
2. 工具配置安全增强
3. 工具层阶段性收尾
4. 本地 manifest 工具目录

## 验证

- `git diff --check` 通过
- 本轮仅涉及文档与协作规范更新，没有新增运行时代码逻辑
