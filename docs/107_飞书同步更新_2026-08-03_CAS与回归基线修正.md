# 2026-08-03 CAS savepoint 与回归基线修正

本轮又补了一项消息事务边界优化：stale generation 的条件更新失败时只回滚本次 savepoint，不再回滚调用方 Unit of Work 中其它已暂存变更。行为测试验证旧回答不会覆盖当前 generation，同时无关 Conversation 变更可以继续提交。

新增 1 项测试后，后端最终回归基线为 `334 tests`，结果 `OK (skipped=3)`；`compileall`、默认 Tool manifest JSON 校验、`git diff --check` 均通过。PostgreSQL/pgvector 集成测试仍因未设置 `TEST_POSTGRES_URL` 跳过。
