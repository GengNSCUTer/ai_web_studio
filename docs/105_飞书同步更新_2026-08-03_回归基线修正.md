# 2026-08-03 回归基线修正

在上一条 Durable 增量同步后，又补充了一个行为测试：验证 DLQ 重放源 Step 时不会绕过另一条仍为 dead-letter 的独立依赖，下游继续保持 `skipped`，Run 继续保持 `dead_letter`。

因此本批最终回归基线从 `330 tests` 更新为 `331 tests`，结果仍为 `OK (skipped=3)`；3 项跳过均为未设置 `TEST_POSTGRES_URL` 的 PostgreSQL/pgvector 集成测试。`compileall`、默认 Tool manifest JSON 校验和 `git diff --check` 仍通过。
