# 2026-05-11 更新：建议记忆重复提取卡住修复

## 本次修复

- 修复用户在长期记忆面板中重复点击“从当前会话提取建议记忆”时的卡住体感。

## 前端变化

- 提取请求进行中时禁止重复触发。
- 再次提取前会清空旧候选，避免旧结果和新请求混在一起。
- 增加 90 秒前端超时兜底。
- 超时或失败后会恢复按钮状态并展示错误提示。

## 验证

- 前端 `npm run lint` 通过。
- 后端 `python -m compileall backend/app` 通过。

