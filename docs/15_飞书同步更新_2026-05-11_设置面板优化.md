# 2026-05-11 更新：设置面板分区优化与受控输入修复

## 本次完成

- 修复长期记忆开关的 React 受控/非受控输入警告。
- 前端对旧设置数据做兼容处理：
  - 缺失 `memory_enabled` 时默认使用 `true`
  - 缺失 `memory_max_chars` 时默认使用 `4000`
- 设置弹窗从单页长表单改为分区式二级菜单。

## 新设置分区

- 模型服务
- 生成参数
- 上下文
- 长期记忆
- 系统提示

## 效果

- 模型、API、生成参数、上下文治理、长期记忆不再全部堆在一个页面。
- 设置面板可读性更好，后续继续增加配置项时也更容易维护。

## 验证

- 前端 `npm run lint` 通过。

